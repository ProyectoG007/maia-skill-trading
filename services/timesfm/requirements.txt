# MAIA — TimesFM Forecast Service — dependencias
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
yfinance>=0.2.40
pandas>=2.0.0
numpy>=1.26.0

# TimesFM (Google Research) — checkpoint descargado de Hugging Face en el primer arranque
timesfm[torch]>=1.2.0
